apiVersion: v1
kind: Service
metadata:
  name: dynatrace-validation
  namespace: dynatrace
  labels:
    dynatrace.com/operator: dynakube
    internal.dynatrace.com/component: validation
spec:
  selector:
    internal.dynatrace.com/app: validation
    internal.dynatrace.com/component: validation
  ports:
    - port: 443
      protocol: TCP
      targetPort: server-port
