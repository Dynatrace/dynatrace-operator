# Copyright Dynatrace LLC
# SPDX-License-Identifier: Apache-2.0

suite: test service account for dynatrace OpenTelemetry collector
templates:
  - Common/otel-collector/serviceaccount-otel-collector.yaml
tests:
  - it: should exist
    set:
      platform: kubernetes
    asserts:
      - isKind:
          of: ServiceAccount
      - equal:
          path: metadata.name
          value: dynatrace-otel-collector
      - equal:
          path: metadata.namespace
          value: NAMESPACE
      - isNotEmpty:
          path: metadata.labels

  - it: should exist with telemetry ingest
    set:
      rbac.extensions.create: false
      rbac.telemetryIngest.create: true
    asserts:
      - isKind:
          of: ServiceAccount
      - equal:
          path: metadata.name
          value: dynatrace-otel-collector
      - equal:
          path: metadata.namespace
          value: NAMESPACE
      - isNotEmpty:
          path: metadata.labels

  - it: shouldn't exist if turned off
    set:
      rbac.extensions.create: false
      rbac.telemetryIngest.create: false
    asserts:
      - hasDocuments:
          count: 0
