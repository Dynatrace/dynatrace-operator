# Copyright Dynatrace LLC
# SPDX-License-Identifier: Apache-2.0

suite: test ServiceAccount for prometheus gateway
templates:
  - Common/prometheus/serviceaccount-prometheus-gateway.yaml
tests:
  - it: should exist
    set:
      experimental.enablePrometheus: true
    asserts:
      - isKind:
          of: ServiceAccount
      - equal:
          path: metadata.name
          value: dynatrace-prometheus-gateway
      - equal:
          path: metadata.namespace
          value: NAMESPACE
      - isNotEmpty:
          path: metadata.labels

  - it: shouldn't exist if prometheus rbac is turned off
    set:
      rbac.prometheus.create: false
      experimental.enablePrometheus: true
    asserts:
      - hasDocuments:
          count: 0

  - it: shouldn't exist if experimental.enablePrometheus is off
    set:
      experimental.enablePrometheus: false
    asserts:
      - hasDocuments:
          count: 0
