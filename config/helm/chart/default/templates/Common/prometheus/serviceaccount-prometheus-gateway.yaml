# Copyright Dynatrace LLC
# SPDX-License-Identifier: Apache-2.0

{{- if .Values.rbac.prometheus.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: dynatrace-prometheus-gateway
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "dynatrace-operator.gatewayLabels" . | nindent 4 }}
{{- end }}
