apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: connectionconfigurations.dynatrace.com
spec:
  group: dynatrace.com
  names:
    kind: ConnectionConfiguration
    listKind: ConnectionConfigurationList
    plural: connectionconfigurations
    singular: connectionconfiguration
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: ConnectionConfiguration is the Schema for the connectionconfigurations
        API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: ConnectionConfigurationSpec defines the desired state of ConnectionConfiguration
          properties:
            apiUrl:
              description: Location of the Dynatrace API to connect to, including
                your specific environment ID
              type: string
            networkZone:
              description: 'Optional: Adds the Operator to the given NetworkZone'
              type: string
            proxy:
              description: 'Optional: Set custom proxy settings either directly or
                from a secret with the field ''proxy'''
              properties:
                value:
                  type: string
                valueFrom:
                  type: string
              type: object
            skipCertCheck:
              description: Disable certificate validation checks for installer download
                and API communication
              type: boolean
            tokens:
              description: Credentials for the Operator to connect back to Dynatrace.
              type: string
            trustedCAs:
              description: 'Optional: Adds custom RootCAs from a configmap'
              type: string
          required:
          - apiUrl
          type: object
        status:
          description: ConnectionConfigurationStatus defines the observed state of
            ConnectionConfiguration
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
