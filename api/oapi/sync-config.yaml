# TODO: Leave comment to leave a example structure with explanations of each field
schemas:
 - name: tenantApiV2
   specUrlEnvVar: "TENANT_API_V2_SPEC_URL" # env var holding the full URL to the spec file
   authEnvVar: "TENANT_API_V2_TOKEN"       # env var holding the Bearer token
   generate:
     packageName: tenantapiv2
     globalProperties:
       models: []
       apis: []
       additional: "" # Additional scheme specific global properties as raw string
