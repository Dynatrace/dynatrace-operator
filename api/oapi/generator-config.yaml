# Default configuration for OpenAPI Generator (Go).
# Per-schema overrides can be set in sync-config.yaml under the 'generate' key.

# renovate datasource=github-releases depName=OpenAPITools/openapi-generator
generatorVersion: v7.22.0
outputDir: pkg/clients/generated
additionalProperties: "generateInterfaces"
globalProperties: "supportingFiles"
